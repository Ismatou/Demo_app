<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Ismatou %><br>
    <%= f.text_field :Ismatou %>
  </div>
  <div class="field">
    <%= f.label :ismatou16@gmail.com %><br>
    <%= f.text_field :ismatou16@gmail.com %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
